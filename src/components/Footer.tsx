const Footer = () => {
  return (
    <footer className="w-full bg-background/95 border-t border-gray-800 py-4 mt-auto">
      <div className="container text-center text-xs text-muted-foreground">
        <p>AliExpress hidden links are now FlyLink — same payment & buyer protection.</p>
      </div>
    </footer>
  );
};

export default Footer;
